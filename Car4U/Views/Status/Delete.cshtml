@model Car4U.Models.Status

@{
    ViewBag.Title = "Delete";
}


<h4>Tem a certeza que pretende apagar este estado?</h4>
<div>
   
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Description)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Description)
        </dd>

    </dl>

    <div class="form-group">
        <div class="col-md-offset-2 col-md-10">

            <div class="button">
                @using (Html.BeginForm())
                {
                    @Html.AntiForgeryToken()
                    <div class="form-actions">
                        <button id="butaoOffice" class="submit" type="submit" value="Delete">Apagar</button>
                    </div>
                }
                <button id="butaoOffice" class="submit" type="button" onclick="location.href='@Url.Action("Index", "Status")'">Voltar</button>
            </div>
        </div>
    </div>
</div>
